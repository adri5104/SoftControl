function move2pos(obj, data, pos)
    
   
    string2send = "w,1";
    for j = 1:9
        string2send = string2send + "," +  num2str( data.inputs(pos, j));
    end

    writeline(obj, string2send);
    
end